
#pragma once

#include "shared_base.h"

namespace engine {
    namespace host {
        std::string username;

        std::string path_materials;
        std::string path_textures;
        std::string path_levels;
        std::string path_screenshots;
        std::string path_plugins;
        std::string path_fonts;
        std::string path_shaders;
        std::string path_sounds;
    }
}